<!DOCTYPE html>
<html lang="en">
<head>
  <title>Twitter</title>
  <link href="/css/site.css" type="text/css" rel="stylesheet" />
</head>
<body>
  <div class="container">
    <ul class="menu">
      <li><a href="/">Home</a></li>
      {{#if currentUser}}
        {{#if isAdmin}}
          <li><a href="/admin/all">Admin Console</a></li>
        {{/if}}
        <a href="/profile/{{currentUser.username}}"><li>{{currentUser.username}}</li></a>
        <li>
          <form id="logout-form" action="/users/logout" method="POST"></form>
          <a href="javascript:document.getElementById('logout-form').submit()">Logout</a>
        </li>
        <li><a href="/tweet">Add a tweet</a></li>
      {{else}}
        <li><a href="/users/register">Register</a></li>
        <li><a href="/users/login">Login</a></li>
      {{/if}}
    </ul>
    {{#if globalError}}
      <h3>{{globalError}}</h3>
    {{/if}}
    {{{body}}}
  </div>
</body>
</html>
